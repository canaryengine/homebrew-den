forked from swiftotter/den

# Den for Homebrew
Homebrew formula for [Den](https://github.com/canaryengine/den). You can install Den using [Homebrew](https://brew.sh) via the following command:

    brew install canaryengine/den/den
